#!/usr/bin/env bash
set -euo pipefail

# Helper to show the SafeLine install command from the lab guide.
# WARNING: this downloads and runs a remote script. Review it carefully before executing.

cat <<'EOF'
# Example installer command (review before running):
# bash -c "$(curl -fsSLk https://waf.chaitin.com/release/latest/manager.sh)" -- --en
EOF

echo "If you want to run the installer, copy the command above, inspect it, then paste and run it in your Ubuntu shell."
